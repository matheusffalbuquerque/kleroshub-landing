apiVersion: v1
kind: Service
metadata:
  name: kleroshub
  namespace: kleroshub
  labels:
    app: kleroshub
spec:
  type: ClusterIP
  selector:
    app: kleroshub
  ports:
    - name: http
      port: 80
      targetPort: http
