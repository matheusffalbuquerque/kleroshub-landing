apiVersion: v1
kind: Service
metadata:
  name: kleroshub
  namespace: kleroshub
  labels:
    app: kleroshub
spec:
  type: NodePort
  selector:
    app: kleroshub
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30310
